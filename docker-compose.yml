version: "3.8"

services:
  web:
    build:
      context: .
    container_name: fastapi-app
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    working_dir: /app
