version: "3.8"

services:
  web:
    build:
      context: .
    container_name: fastapi-app
    ports:
      - "8000:8000"  # 外部には公開しない（Nginx経由でアクセス）
    volumes:
      - ./app:/app
    working_dir: /app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
