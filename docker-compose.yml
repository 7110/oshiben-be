version: "3.8"

services:
  web:
    build:
      context: .
    env_file:
      - .env
    container_name: fastapi-app
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    working_dir: /app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
